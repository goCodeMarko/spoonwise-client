import { LineClampPipe } from './line-clamp.pipe';

describe('LineClampPipe', () => {
  it('create an instance', () => {
    const pipe = new LineClampPipe();
    expect(pipe).toBeTruthy();
  });
});
