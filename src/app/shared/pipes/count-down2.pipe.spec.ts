import { CountDown2Pipe } from './count-down2.pipe';

describe('CountDown2Pipe', () => {
  it('create an instance', () => {
    const pipe = new CountDown2Pipe();
    expect(pipe).toBeTruthy();
  });
});
